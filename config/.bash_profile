# bcftools
export PATH=/apps/terminal_dip/bcftools/bin:$PATH

# Plink2
export PATH=/apps/terminal_dip/plink2:$PATH

# Shapeit5
export PATH=/apps/terminal_dip/shapeit5:$PATH

# impute5
export PATH=/apps/terminal_dip/impute5:$PATH

# Load the default .profile
[[ -s "$HOME/.profile" ]] && source "$HOME/.profile" # Load the default .profile
